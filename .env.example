# Warung Laras — salin menjadi .env (di root atau di folder server)
# lalu sesuaikan kredensial PostgreSQL-mu.

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/warung_laras?schema=public"

# Port server Node (REST + WebSocket)
PORT=4000
